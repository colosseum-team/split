# FSD Structure

- `app` - app bootstrap, providers, global config
- `pages` - route-level screens
- `widgets` - composed blocks for pages
- `features` - user actions and business scenarios
- `entities` - domain entities (models, ui, api)
- `shared` - reusable ui/lib/config/api helpers
