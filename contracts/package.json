{
  "name": "@escros/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Anchor escrow program for the Escros freelance platform",
  "scripts": {
    "build": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "test": "anchor test --skip-deploy --skip-build",
    "idl:sync": "node ./scripts/sync-idl.mjs"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.98.4"
  }
}
