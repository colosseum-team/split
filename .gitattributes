# Default: let Git decide based on content.
* text=auto

# Shell scripts and Husky hooks must keep LF endings, otherwise sh.exe on
# Windows can choke on the shebang ("#!/usr/bin/env sh\r" -> "sh\r not found").
*.sh        text eol=lf
.husky/*    text eol=lf
